{
  "name": "piflow",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "pnpm --parallel --filter piflow-web --filter piflow-server run dev",
    "build": "pnpm --filter @piflow/protocol run build && pnpm --filter piflow-server run build && pnpm --filter piflow-web run build",
    "start": "pnpm --filter piflow-server run start",
    "lint": "eslint . --max-warnings 0 && stylelint 'apps/web/src/**/*.css' --max-warnings 0",
    "lint:fix": "eslint . --fix && stylelint 'apps/web/src/**/*.css' --fix",
    "typecheck": "pnpm --filter @piflow/protocol run typecheck && pnpm --filter piflow-web run typecheck && pnpm --filter piflow-server run typecheck",
    "test": "pnpm --filter piflow-server run test",
    "check": "pnpm lint && pnpm typecheck && pnpm test && pnpm build"
  },
  "devDependencies": {
    "@antfu/eslint-config": "^9.2.0",
    "@eslint-react/eslint-plugin": "^5.6.0",
    "@types/react": "^19.1.0",
    "@types/react-dom": "^19.1.0",
    "eslint": "^10.8.0",
    "eslint-plugin-react-refresh": "^0.5.0",
    "stylelint": "^17.14.1",
    "stylelint-config-standard": "^40.0.0"
  }
}
